function complex_symbols = apsk_modulate(frame)
%psk_modulate Maps the bits in a stream of bits to an array of complex
%symbols following an 16APSK modulation scheme.
    complex_symbols = frame;
end

