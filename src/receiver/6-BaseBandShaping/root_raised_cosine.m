function Hd = root_raised_cosine
%ROOT_RAISED_COSINE Returns a discrete-time filter object.

% MATLAB Code
% Generated by MATLAB(R) 9.9 and DSP System Toolbox 9.11.
% Generated on: 29-Dec-2020 23:40:49

% FIR Window Raised-cosine filter designed using the FIRRCOS function.

% All frequency values are in MHz.
Fs = 48;  % Sampling Frequency

N  = 98;         % Order
Fc = 1.7;        % Cutoff Frequency
TM = 'Rolloff';  % Transition Mode
R  = 0.2;        % Rolloff
DT = 'sqrt';     % Design Type

% Create the window vector for the design algorithm.
win = hamming(N+1);

% Calculate the coefficients using the FIR1 function.
b  = firrcos(N, Fc/(Fs/2), R, 2, TM, DT, [], win);
Hd = dfilt.dffir(b);

% [EOF]
